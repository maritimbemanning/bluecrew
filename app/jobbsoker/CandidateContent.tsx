"use client";

// Deprecated: This file previously contained a large candidate form component.
// The new implementation lives in `app/jobbsoker/CandidateForm.tsx` and is used by the route.
// We keep this stub to avoid type-checking/build issues while preserving git history.

export default function CandidateContent() {
  return null;
}

